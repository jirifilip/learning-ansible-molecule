ansible-navigator:
  ansible-builder:
    workdir: /tmp/

  ansible-runner:
    artifact-dir: /tmp/test1

  playbook-artifact:
    enable: True
    replay: /tmp/test_artifact.json
    save-as: /tmp/test_artifact.json
